project(Chakra)

define_dll(
	Chakra 
	LINK ${CHAKRACORE_LIB}
	RUNTIME ${CHAKRACORE_DLL}
	DEFINE CHAKRA_EXPORTS
)

if (${BEDROCKPY_VERBOSE_MODE})
	cmake_print_properties(
		TARGETS Chakra
		PROPERTIES
			NAME
			INSTALL_RPATH
			BINARY_DIR
			INCLUDE_DIRECTORIES
			LINK_LIBRARIES
			CXX_STANDARD
			CXX_STANDARD_REQUIRED
			COMPILE_DEFINITIONS
			COMPILE_FLAGS
			COMPILE_OPTIONS
	)
endif ()